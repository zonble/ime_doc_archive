# 切換輸入模式

漢音提供四種輸入模式（漢音輸入模式﹑全形輸入模式﹑英數輸入模式﹑地址輸入模式）。關於此四種輸入模式請參考輸入模式。

四種模式切換的方法如下；

- 【鍵盤】：按〔Ctrl+`〕鍵。
- 【滑鼠】：點按狀態按鈕，直到出現出現想要的模式代表符號。

例如要選擇地址模式：

- 【鍵盤】：按〔Ctrl+\`〕鍵，直到狀態按鈕出現「地」。如欲回到漢音模式，如上所述，按〔Ctrl〕+〔`〕鍵，直到狀態按鈕出現「中」。
- 【滑鼠】：點按狀態按鈕，直到出現「地」字。

注意：可以直接按[Shift]鍵切換英數半形與漢音模式。
